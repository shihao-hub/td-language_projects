---
name: project-inventory
description: 维护 MONOREPO.md（个人项目价值盘点表）的完整工作流。当用户要求"登记新项目"、"更新盘点表"、"MONOREPO.md 加一条/补全"、"盘点表同步归档或迁移变动"时必须使用本 skill，变体说法（"把新项目记到盘点里"、"盘点表过时了"、"盘点表和实际对不上了"）也要触发。核心是按目录归节、六列统一结构、AI 起草摘要与人工填主观列的分工，以及项目增删/归档/迁移时与本表的联动更新纪律——结构变了表不变，表就烂了。
---

# Skill: project-inventory

# 项目价值盘点维护（MONOREPO.md）

## 概述

`MONOREPO.md` 是父仓库根目录的**个人项目价值盘点表**：主观文档（个人成长/简历视角），不是客观项目索引。覆盖 4 个语言子仓（`go_projects/`、`python_projects/`、`rust_projects/`、`typescript_projects/`）与归档区（`.archived/<lang>/`）的全部项目。

维护的高层流程：

1. 存量对照（目录实况 vs 表内容，只读）
2. 摘要起草（AI 依据 README，用户逐条过目）
3. 更新文档（原样迁移 + 新条目 + 格式验证）
4. 联动同步与提交

## 你的任务

先判断用户处在哪个场景，再跳入对应位置：

- 新项目登记 → 跳过对照，直接从「阶段 2：摘要起草」开始
- 补全/核对存量盘点表 → 从「阶段 0」开始
- 刚发生项目归档（走 [SKILLARCHIVE.md](SKILLARCHIVE.md)）/迁移/改名 → 查「联动场景速查」执行对应动作
- 表格渲染错列/格式可疑 → 直接跑「阶段 3」的验证脚本

任何场景都不要代填主观列——状态、源码阅读情况、价值、下一步是表主人的个人判断，AI 代填等于编造，会让整张表失去可信度。

## 核心纪律

理解规则背后的原因，比死记步骤更重要：

1. **按目录归节，不按语言归属**。项目写进它实际所在的目录那一节：`python-launcher-go` 代码是 Go，但目录在 `go_projects/` 下，就登记在 go 节。按目录走才能「扫一遍目录就核对出表是否齐全」，不产生归属争议。
2. **六列统一，任何节不例外**。列结构固定为 `项目 | 摘要 | 状态 | 源码阅读情况 | 价值 | 下一步`。归档节**不得**简化成只有摘要——归档可能只是暂时的，重启评估需要全列信息。
3. **主观列只由表主人填**。状态、源码阅读情况、价值、下一步一律留空待填。
4. **摘要可由 AI 起草，但必须用户过目**。依据 README 首段（无 README 则看入口文件推断），15~40 字，客观精炼，说明「是什么 + 干什么用」，英文翻译成中文，拒绝营销式语言。不确认不落盘。
5. **盘点表是仓库结构的从动方**。项目新增、归档、迁移、改名时，本表必须联动更新。
6. **每行管道符数一致**。六列表每行恰 7 个 `|`，多一个少一个都会错列渲染且肉眼难察觉，写完必须脚本验证。

## 阶段 0：存量对照（只读，先出计划再动手）

1. **目录实况**：列出 4 个子仓 + `.archived/<lang>/` 的全部目录。
2. **差异对照**：实际有、表里无 → 缺登条目；表里有、实际无 → 已删除/改名，条目待清理；条目所在节与实际目录不符 → 待迁移（如写在 python 节的 `python-launcher-go`）。
3. **过时内容识别**：表里的待办已经发生（如「需要处理掉」的项目实际已归档）、状态明显过期 → 列出让用户裁决。
4. **向用户展示**：差异清单 + 补全计划 + 待确认问题（列结构、归档节详略、条目归属），**确认后才动文件**。

## 阶段 1：摘要起草（可派发代理）

缺摘要的项目批量读 README 起草（项目多时派 explore 代理并行，活跃区与归档区各一个）：

- 有 README 用 README 首段；README 为空/是笔记体（如 `instancelock`）则看入口源码推断，并在结果里标注「依据：推断」。
- 无法给出实质描述的（如空壳 `taskmon-go`）如实写「空壳项目，刚初始化」，并提醒用户确认定位。
- 起草结果以清单形式呈给用户逐条确认。

## 阶段 2：更新文档

- 表头说明块保留（列含义 + 归档不简化原因），标题用实际目录名（`typescript_projects`，不是 `typescripts`）。
- 已有条目内容**原样迁移**，不趁便改写用户的主观表述；时间性内容（「期待 9.15 左右开始」）归入「下一步」列。
- 新条目只填「摘要」，主观列留空。
- 归档项目放「归档项目（.archived）」大节，按 `### .archived/<lang>` 子标题分组。

## 阶段 3：格式验证与提交

验证脚本（六列表每行必须 7 个 `|`，输出为空即通过，有输出则逐行修复）：

```powershell
$lines = Get-Content -LiteralPath MONOREPO.md
$lines | Where-Object { $_ -match '^\|' -and (([regex]::Matches($_,'\|')).Count -ne 7) }
```

`README.md` 已有「项目盘点」节链接 `MONOREPO.md`；仅当文档结构级变化（新增/更名本表）时才需要动 README。

## 联动场景速查

| 触发事件 | 本表动作 |
|---|---|
| 子仓新增项目 | 对应语言节加行，摘要按 README 起草，主观列留空待填 |
| 项目归档（SKILLARCHIVE.md 流程） | 活跃节删行 + `.archived/<lang>` 对应子节加行（全列保留） |
| 归档项目重启 | 归档子节删行 + 对应语言节加行 |
| 项目改名/跨子仓迁移 | 行随实际目录移到对应节 |
| README 重写导致摘要失真 | 仅更新摘要列，主观列不动 |

## 提交信息格式

**示例 1：**
Input: 把 rust_projects 下新项目 foo 登记到盘点表
Output: `docs: 盘点表登记新项目 foo`（仅填摘要列，主观列留空）

**示例 2：**
Input: 刚按 SKILLARCHIVE.md 归档了 sublime-folders
Output: `docs: 盘点表同步项目归档变动`（go 节删行 + `.archived/go_projects` 子节加行，全列保留）

**示例 3：**
Input: 盘点表和实际目录对不上了，全量核对补全
Output: `docs: 完善项目价值盘点文档`

## 陷阱速查

| 陷阱 | 规避 |
|---|---|
| AI 代填主观列（编造阅读进度/价值） | 主观列一律留空，只起草摘要且逐条过目 |
| 按语言而非目录归节 | 以目录实况为准，python-launcher-go 归 go 节 |
| 已处理条目残留（"待处理"其实已处理） | 阶段 0 对照实况，过时表述列出让用户裁决 |
| 归档节简化成两列 | 保留六列，归档可能只是暂时 |
| 管道符不齐导致错列 | 阶段 3 的脚本验证，输出为空才算过 |
| 摘要抄 README 太长/带营销味 | 15~40 字，"是什么 + 干什么用"，翻译成中文 |
| 时间性内容混进状态列 | 状态列放结论，计划类内容进「下一步」 |
| 趁便改写用户已有的主观表述 | 原样迁移，只有用户明确要改才改 |
