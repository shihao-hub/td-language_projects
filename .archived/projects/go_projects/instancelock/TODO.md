# instancelock TODO

## 通知信道：把第二实例的信息递给持锁进程

当前行为：第二实例拿锁失败只能自行退出（打日志），持锁进程无感知。
目标：第二实例被拒时把 argv / 消息递给持锁进程。

- 先例：Electron 原生单实例锁 = named mutex（互斥）+ 命名管道（回传 argv）
- 设想：新增 `notify` 子命令——拿锁失败后改为通过信道把参数发给持锁进程再退出；`hold` 进程加可选 `--listen` 开启监听（回调 / 打印到 stdout）
- Windows 实现可选命名管道（`\\.\pipe\...`）或本地 TCP 回环；跨平台需再评估（Unix domain socket）

## Windows 原生互斥体后端评估

对比记录：

| | 文件锁（当前） | named mutex |
|---|---|---|
| 内核对象 | 否（锁文件 + 重命名/独占句柄） | 是 |
| 残留 | 锁文件可能残留（需清理逻辑） | 零残留（进程退出内核自动回收） |
| 跨平台 | 统一 | Windows 专属，需按平台分支 |
| 跨用户/会话 | 取决于锁文件位置 | Global\ 前缀可跨会话 |

评估是否加 Windows 原生互斥体后端（build tag 分支），保持非 Windows 走文件锁。

## ~~list 命令输出 JSON~~（已完成：全部命令已迁移 JSON 包络协议，见 README）
