# exe-launcher

**EXE 启动器**：把常用的 exe 集中管理，一键启动 / 打开目录 / 开 PowerShell。
纯 Win32 API 实现（Go + syscall），单文件零依赖。

## 功能

- 手动添加 exe，或扫描目录批量导入（勾选确认，不自动入库）
- 一键启动（ShellExecute，支持 UAC 提权程序）、打开所在目录、在目录开 PowerShell
- 失效条目红字、一键清理；状态栏实时统计
- **打标**：每条记录至多一个系统标签 + 一个用户标签
  - 系统标签：`待完善` / `待验证` / `有问题` / `稳定`，固定单选，标签列按标签着色（橙/蓝/红/绿）
  - 用户标签：自由文本，作为记录的描述
  - 工具栏「打标」按钮或右键「设置标签」打开打标对话框（Enter 确定 / Esc 取消）
  - 工具栏右侧可按系统标签筛选列表（只看待完善等），状态栏统计待完善数量
- **改路径**：exe 移动位置后只更新路径，标签与添加时间保留，名称跟随新文件名；工具栏「改路径」按钮或右键「修改路径」，文件选择框默认打开旧路径所在目录
- **单实例**：重复启动会提示并自动激活已运行的窗口
- **查看介绍**：为 exe 配套同目录同名 `.md`（如 `foo.exe` → `foo.md`），工具栏或右键「介绍」即弹出渲染后的 Markdown 介绍窗口
- 右键菜单与工具栏共用同一套命令

## 数据

配置与日志都落在 `%AppData%\exe-launcher\`：

- `config.json` 条目列表（名称 / 路径 / 添加时间 / 系统标签 `sys_tag` / 用户标签 `user_tag`）
- `exe-launcher.log` 运行日志（panic / 关键动作全记录）

## 给 exe 写介绍

在 exe 同目录放一个同名 md 即可。支持标题、粗体、斜体、行内代码、
代码块、列表、引用、分隔线和链接。
